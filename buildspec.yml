version: 0.2
phases:
  install:
    commands:
      - npm install
      - npm install -g @angular/cli
  build:
    commands:
      - ng build --configuration=production
      - ls dist/aws-angular/
artifacts:
  base-directory: dist/aws-angular/browser
  files:
    - '**/*'